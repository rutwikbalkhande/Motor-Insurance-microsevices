server:
  port: 8083

spring:
  application:
    name: policy-service

  datasource:
    url: jdbc:mysql://mysql:3306/policy_motor_insurance
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

# Prometheus application.yml
management:
  endpoints:
    web:
      exposure:
        # include: health,info,metrics,prometheus  # expose Prometheus endpoint use any 1 '*' select all
        include: '*'
  endpoint:
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true  # Enable Prometheus metrics export

# Kafka

